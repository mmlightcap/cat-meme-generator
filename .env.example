# Generate with: npx auth secret
AUTH_SECRET=

# PostgreSQL connection string (DigitalOcean managed DB)
DATABASE_URL=

# Parallel.ai — web search API for creator sourcing
PARALLEL_API_KEY=

# Set to your app's public URL in production
NEXTAUTH_URL=http://localhost:3000
